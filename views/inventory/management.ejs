<%# Unit 4 Assignment %>
<% if (title) { %>
    <h1><%= title %></h1>
    <% } else {
      res.redirect('/')
    } %>

<%= messages() %>

<% if (errors) { %>
    <ul class="notice">
   <% errors.array().forEach(error => { %>
     <li><%= error.msg %></li>
  <%  }) %>
   </ul>
  <% } %>

<a href="./add-classification" title="Click to add new Classification">Add New Classification</a><br>
<a href="./add-inventory" title="Click to add new Inventory">Add New Inventory</a>

